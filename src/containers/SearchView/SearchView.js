import React, { Component } from 'react';
import { View, Text, SearchBar } from 'react-native';

class SearchView extends Component {
  render() {
    return (
      <View style={{ flex: 1 }}>
        <Text style={{ flex: 1 }}>SearchView</Text>
      </View>
    );
  }
}

export default SearchView;
